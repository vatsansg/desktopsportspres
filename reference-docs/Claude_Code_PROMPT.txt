You are building the "LED Asset Download and Synchronisation Application" — a Windows desktop
app (Python/Flask backend, local SQLite, local Windows UI) that is the downstream companion to
the already-completed Asset Management Web Application. This app only ever reads from the web
app's existing Azure Storage Account; it does not provision any Azure resources of its own.

STEP 0 — Before writing any code, read these four documents in full (I have copied them into
this repo under reference-docs\ — if they are not there yet, tell me and I will add them):

  reference-docs\LED_Asset_Download_Desktop_Application_BRD.pdf   (v1.2 + Addendum A — the requirements)
  reference-docs\LED_Desktop_App_Implementation_Sequence-1.md      (the phase/step build order)
  reference-docs\Desktop_Application_Solution_Implementation_Plan.md  (the working process)
  reference-docs\workflow.md                                       (living progress tracker — update this file in place after every phase, never create a new one)

Read the BRD's Section 39 (Addendum A) carefully — it amends and supersedes parts of Section 36.

STEP 1 — Kickoff decisions already confirmed by the project owner (do not re-ask these):
  - Storage Account: shared with the web application (same account, not a separate one).
  - A real test event already exists in it: Event ID 1000, "Star contender Doha" (exported from
    the web app's genuine Export Event function on 18 Sep 2026). Use it for all development from
    Phase 3 onward. Build against the cloud folder naming confirmed in Section 39.1 (e.g.
    "Table 1/Inner"), not any local mirror's casing.
  - Venue network for development: local/simulated Windows shared folders for now — I will give
    you the actual local folder paths before Phase 5 starts. The authentication method for the
    real venue network is still open (BRD Section 36) — do not assume one.
  - Email: Azure Communication Services (same as the web app). New business rule: if there is no
    internet connectivity when a notification would be sent, skip it silently and log "notification
    skipped — no connectivity" — do not treat this as an operation failure.
  - GitHub: this repo, https://github.com/vatsansg/desktopsportspres. Mirror the web application's
    branch strategy and commit message convention.
  - Installer/updates: produced to the desktopinstaller container in the same Storage Account
    (https://sasportspresentation.blob.core.windows.net/desktopinstaller). No code-signing required.

  Do NOT ask me for the Storage Account access key yet — you don't need it until Phase 4. When you
  reach Phase 4, stop and ask me for it directly; I will provide it then. Store it only in this
  repo's own local .env (or equivalent), and add that file to .gitignore before your very first
  commit in Phase 0. Never ask me to paste it into a document that will be committed.

  Everything else still listed as open in BRD Section 36 (timestamp cut-off/timezone definition,
  max tables per event, ping-testing vs. folder-access testing, shared-folder auth method for the
  real venue, retry behaviour, checksum validation, asset retention policy, DB schema migration
  approach, multi-admin support, local change log filename) is genuinely undecided — flag each one
  to me at the specific step that needs it, per the Implementation Sequence document's own inline
  notes. Do not assume answers.

STEP 2 — Working process, for every phase (Phase 0 through Phase 14, per the Implementation
Sequence document):
  1. Summarize what the phase requires and ask any clarifying questions before writing code.
  2. Build the phase, validating against that step's "Validate before moving on" criteria.
  3. Apply the wtt-brand skill to every screen/dialog in this Windows UI.
  4. Re-run wtt-brand and ralph-loop once the phase is functionally complete, to polish the UI.
     If ralph-loop is not available at the project level, ask me rather than substituting something else.
  5. Create a QA Test Case document for the phase (follow QA_Test_Case_Template.md, adapted for a
     desktop app — tests run on a Windows machine, not a browser).
  6. Do an independent solution-architect review of the phase (and cumulatively everything before it).
  7. Update workflow.md in place: mark the phase complete, note what was actually built, and the date.
  8. Hand off to me: phase summary, QA Test Case doc, updated workflow.md, and — for phases with UI —
     a short demo/screenshots, plus confirmation of what was validated against the real (or agreed
     test) Storage Account.
  9. Wait for my go-ahead before starting the next phase.
  10. On go-ahead, commit to GitHub with a commit message matching the phase description, then proceed.

STEP 3 — Security: treat every hand-off as a "release" against Security_Checklist_Template.md,
paying particular attention to: the accepted-risk plain-text local admin credential (BRD Section
6.1 — already accepted, but confirm it hasn't silently expanded in scope), protection of the
Storage Account access key in local configuration, and the security of Windows shared-folder
credentials used for device push.

Start with Phase 0 (Step 0.1 — repository/environment scaffold, Step 0.2 — empty SQLite schema)
now. Summarize your plan for Phase 0 before you write anything.