# Build-only (Phase 13): freezes the app into installer\ledsync.spec's two executables.
# Not installed on a venue machine - only needed on the machine that builds the installer.
-r requirements.txt
pyinstaller==6.22.3
